# Dori-iOS 헤더 규칙

- 신규 생성 파일(`.swift`, `.xcconfig`)에는 아래 템플릿을 파일 최상단에 추가한다.
- 작성자명은 AI 사용 여부와 무관하게 반드시 `강동영`으로 유지한다.

```swift
//
//  {FileName}
//  Dori-iOS
//
//  Created by 강동영 on {M/d/yy}.
//
```
