# Git Worktree 워크플로우

## Stable Rules

- 워크트리는 메인 레포 내부가 아니라 형제 폴더에 둔다.
- 워크트리 생성과 제거는 메인 레포에서 실행한다.
- 보안 설정 파일과 공통 설정 파일 존재 여부를 생성 직후 확인한다.

## Why

이 규칙은 git 구조 오염과 설정 누락을 막기 위한 운영 규칙이다. 경로 예시보다 원칙을 우선한다.
